<odoo>

    <record id="siso_project_action" model="ir.actions.act_window">
        <field name="name">Projets</field>
        <field name="res_model">siso.project</field>
        <field name="view_mode">tree,form</field>
    </record>

    <record id="siso_project_tree" model="ir.ui.view">
        <field name="model">siso.project</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="date"/>
                <field name="technologies"/>
            </list>
        </field>
    </record>

    <record id="siso_project_form" model="ir.ui.view">
        <field name="model">siso.project</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="date"/>
                        <field name="technologies"/>
                        <field name="link"/>
                        <field name="image"/>
                        <field name="description"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>